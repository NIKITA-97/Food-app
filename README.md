react

there are 2 types of routing
1. Client side routing -- don't make network calls, everything is already there
2. server side routing  -- make network calls

single page application --  it's a single page, everything else is component